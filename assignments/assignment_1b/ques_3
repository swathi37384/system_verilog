Why can’t we use a foreach loop for popping all the elements in a
queue?
ans:

Because foreach is meant for traversing the elements of a collection, while pop_front() or pop_back() changes the size of the queue.

foreach → use when you want to access/traverse elements without changing the collection's size.

while + pop_front() → use when you want to remove all elements.
